"use client"; import { useState } from "react";
export default function CvPage(){const [message,setMessage]=useState<string>(); async function upload(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);const response=await fetch('/api/cv',{method:'POST',body:fd});const data=await response.json();setMessage(data.error??data.message)}return <><p className="eyebrow">Step 1 of 2</p><h1>Turn your CV into your job brief.</h1><p className="muted">PDF or DOCX, up to 8MB. We extract only information supported by your CV.</p><form className="panel" onSubmit={upload} style={{maxWidth:650,marginTop:25}}><div className="field"><label htmlFor="file">CV file</label><input id="file" name="file" type="file" accept=".pdf,.docx,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document" required/></div><button className="btn btn-primary" style={{marginTop:18}}>Upload and analyze</button>{message&&<p className="muted" role="status">{message}</p>}</form><section className="panel" style={{maxWidth:650,marginTop:18}}><b>Your data controls</b><p className="muted">Production setup uses authenticated, private storage and lets you delete your CV and candidate profile. No CV content is logged by the application.</p></section></>}
