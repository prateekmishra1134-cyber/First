"use client"; import { useState } from "react"; import { useRouter } from "next/navigation";
export default function Search(){const r=useRouter();const [error,setError]=useState('');function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const roles=String(f.get('roles')).split(',').map(x=>x.trim()).filter(Boolean);if(!roles.length){setError('Add at least one role.');return}sessionStorage.setItem('criteria',JSON.stringify({roles,locations:String(f.get('locations')).split(',').map(x=>x.trim()).filter(Boolean),workMode:f.get('workMode'),experience:f.get('experience')||null,salaryMin:null,salaryMax:null,currency:null,industries:[],companies:[],searchFortune500:f.get('fortune')==='on'}));r.push('/jobs')}return <><p className="eyebrow">Step 2 of 2</p><h1>What are you looking for?</h1><p className="muted">Use commas to add several roles or locations. City, country, remote, and worldwide are all supported.</p><form className="panel" onSubmit={submit} style={{maxWidth:830,marginTop:25}}><div className="form-grid"><div className="field"><label>Job roles *</label><input name="roles" placeholder="Business Analyst, SAP Consultant" required/></div><div className="field"><label>Locations</label><input name="locations" placeholder="Bangalore, India, Remote"/></div><div className="field"><label>Work mode</label><select name="workMode" defaultValue="any"><option value="any">Any</option><option value="remote">Remote</option><option value="hybrid">Hybrid</option><option value="on_site">On-site</option></select></div><div className="field"><label>Experience</label><select name="experience"><option value="">Any level</option><option>Internship</option><option>Entry level</option><option>0–2 years</option><option>2–5 years</option><option>5–10 years</option><option>10+ years</option></select></div><div className="field"><label>Industries (optional)</label><input placeholder="Finance, Technology"/></div><div className="field"><label>Target companies (optional)</label><input placeholder="Company names"/></div></div><label style={{display:'block',marginTop:19,fontWeight:700}}><input name="fortune" type="checkbox"/> Search supported Fortune 500 company careers pages</label><button className="btn btn-primary" style={{marginTop:20}}>Find legitimate jobs</button>{error&&<p role="alert">{error}</p>}</form></>}
