# Required for production AI analysis and database persistence
OPENAI_API_KEY=
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Optional: server-side web-search provider (must be an approved provider/API)
BRAVE_SEARCH_API_KEY=
# Set only for local development to enable fixture results; never use in production
ENABLE_DEVELOPMENT_FIXTURES=false
