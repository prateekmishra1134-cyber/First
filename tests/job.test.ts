import { describe,it,expect } from 'vitest'; import { deduplicateJobs } from '@/lib/utils/job'; import { searchCriteriaSchema,candidateProfileSchema } from '@/lib/validation/schemas'; import { heuristicMatch } from '@/lib/search/pipeline'; import type { Job } from '@/types/domain';
const job:Job={title:'Business Analyst',company:'Acme',location:'Mumbai',country:'India',remoteType:'hybrid',employmentType:null,salary:null,description:'Excel and SAP experience',requirements:[],skills:['SAP'],source:'company-careers',sourceUrl:'https://acme.test/job?utm_source=x',applicationUrl:'https://acme.test/apply',postedDate:null,discoveredDate:'2026-01-01',companyWebsite:null,isFortune500:false,fortune500Rank:null};
describe('job safety',()=>{it('deduplicates tracking-url variants',()=>expect(deduplicateJobs([job,{...job,source:'indeed',sourceUrl:'https://elsewhere.test'}])).toHaveLength(1));it('validates salary bounds',()=>expect(()=>searchCriteriaSchema.parse({roles:['Analyst'],locations:[],workMode:'any',experience:null,salaryMin:20,salaryMax:10,currency:'USD',industries:[],companies:[],searchFortune500:false})).toThrow());it('only matches CV-supported skills',()=>{const p=candidateProfileSchema.parse({fullName:null,education:[],experience:[],skills:['SAP'],technicalSkills:['Excel'],softSkills:[],certifications:[],projects:[],industries:[],languages:[],locations:[],likelyRoles:[],seniority:null,yearsExperience:null,keywords:[],atsKeywords:[],strengths:[],potentialWeaknesses:[]});expect(heuristicMatch(p,job,{roles:['Business Analyst'],locations:[],workMode:'any',experience:null,salaryMin:null,salaryMax:null,currency:null,industries:[],companies:[],searchFortune500:false}).matchingSkills).toEqual(['SAP','Excel']);});});
